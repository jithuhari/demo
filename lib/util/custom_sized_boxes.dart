

import 'package:flutter/cupertino.dart';

class SizedBoxes extends StatelessWidget {
  ///SizedBox with Height 5
  const SizedBoxes({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}

class SizedBoxH2 extends StatelessWidget {
  ///SizedBox with Height 5
  const SizedBoxH2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 2,
    );
  }
}

class SizedBoxH5 extends StatelessWidget {
  ///SizedBox with Height 5
  const SizedBoxH5({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 5,
    );
  }
}

class SizedBoxH10 extends StatelessWidget {
  ///SizedBox with Height 10
  const SizedBoxH10({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  SizedBox(
      height: 10,
    );
  }
}

class SizedBoxH15 extends StatelessWidget {
  ///SizedBox with Height 15
  const SizedBoxH15({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 15,
    );
  }
}

class SizedBoxH20 extends StatelessWidget {
  ///SizedBox with Height 20
  const SizedBoxH20({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 20,
    );
  }
}

class SizedBoxH24 extends StatelessWidget {
  ///SizedBox with Height 20
  const SizedBoxH24({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 24,
    );
  }
}

class SizedBoxH30 extends StatelessWidget {
  ///SizedBox with Height 30
  const SizedBoxH30({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 30,
    );
  }
}

class SizedBoxH40 extends StatelessWidget {
  ///SizedBox with Height 30
  const SizedBoxH40({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 40,
    );
  }
}

class SizedBoxH44 extends StatelessWidget {
  ///SizedBox with Height 30
  const SizedBoxH44({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 44,
    );
  }
}

class SizedBoxH50 extends StatelessWidget {
  ///SizedBox with Height 30
  const SizedBoxH50({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 50,
    );
  }
}

class SizedBoxH70 extends StatelessWidget {
  ///SizedBox with Height 70
  const SizedBoxH70({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 70,
    );
  }
}

class SizedBoxH80 extends StatelessWidget {
  ///SizedBox with Height 80
  const SizedBoxH80({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 80,
    );
  }
}
class SizedBoxH100 extends StatelessWidget {
  ///SizedBox with Height 80
  const SizedBoxH100({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 100,
    );
  }
}
class SizedBoxH125 extends StatelessWidget {
  ///SizedBox with Height 80
  const SizedBoxH125({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 125,
    );
  }
}
class SizedBoxH150 extends StatelessWidget {
  ///SizedBox with Height 80
  const SizedBoxH150({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 150,
    );
  }
}

class SizedBoxW5 extends StatelessWidget {
  ///SizedBox with Width 5
  const SizedBoxW5({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 5,
    );
  }
}

class SizedBoxW10 extends StatelessWidget {
  ///SizedBox with Width 10
  const SizedBoxW10({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 10,
    );
  }
}

class SizedBoxW12 extends StatelessWidget {
  ///SizedBox with Width 10
  const SizedBoxW12({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 12,
    );
  }
}

class SizedBoxW15 extends StatelessWidget {
  ///SizedBox with Width 15
  const SizedBoxW15({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 15,
    );
  }
}

class SizedBoxW20 extends StatelessWidget {
  ///SizedBox with Width 20
  const SizedBoxW20({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 20,
    );
  }
}

class SizedBoxW24 extends StatelessWidget {
  ///SizedBox with Width 24
  const SizedBoxW24({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 24,
    );
  }
}

class SizedBoxW30 extends StatelessWidget {
  ///SizedBox with Width 30
  const SizedBoxW30({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 30,
    );
  }
}
