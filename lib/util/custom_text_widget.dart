import 'package:flutter/material.dart';

import '../resources/app_colors.dart';

///fonts weights
const FontWeight kFW800 = FontWeight.w800;
const FontWeight kFW700 = FontWeight.w700;
const FontWeight kFW600 = FontWeight.w600;
const FontWeight kFW500 = FontWeight.w500;
// const _k500 = FontWeight.w500;
const FontWeight kFW400 = FontWeight.w400;
const FontWeight kFW300 = FontWeight.w300;

class TextPOP36W500 extends StatelessWidget {
  ///Poppins Size: 36, Weight: [kFW400], Default Color: AppColors.textBlackColor
  const TextPOP36W500(
    this.text, {
    Key? key,
    this.color,
    this.textAlign,
  }) : super(key: key);
  final String text;
  final Color? color;
  final TextAlign? textAlign;

  @override
  Widget build(BuildContext context) {
    return Text(
      text,
      textAlign: textAlign,
      style: TextStyle(
        fontSize: 36,
        fontWeight: kFW400,
        color: color ?? AppColors.textBlackColor,
      ),
    );
  }
}

class TextPOP36W700 extends StatelessWidget {
  ///Poppins Size: 36, Weight: [kFW700], Default Color: AppColors.textBlackColor
  const TextPOP36W700(
    this.text, {
    Key? key,
    this.color,
    this.textAlign,
  }) : super(key: key);
  final String text;
  final Color? color;
  final TextAlign? textAlign;

  @override
  Widget build(BuildContext context) {
    return Text(
      text,
      textAlign: textAlign,
      style: TextStyle(
        fontSize: 36,
        fontWeight: kFW700,
        color: color ?? AppColors.textBlackColor,
      ),
    );
  }
}

class TextPOP32W700 extends StatelessWidget {
  ///Poppins Size: 32, Weight: [kFW700], Default Color: AppColors.textBlackColor
  const TextPOP32W700(
    this.text, {
    Key? key,
    this.color,
    this.textAlign,
  }) : super(key: key);
  final String text;
  final Color? color;
  final TextAlign? textAlign;

  @override
  Widget build(BuildContext context) {
    return Text(
      text,
      textAlign: textAlign,
      style: TextStyle(
        fontSize: 32,
        fontWeight: kFW700,
        color: color ?? AppColors.textBlackColor,
      ),
    );
  }
}

class TextPOP32W600 extends StatelessWidget {
  ///Poppins Size: 32, Weight: [kFW600], Default Color: AppColors.textBlackColor
  const TextPOP32W600(
    this.text, {
    Key? key,
    this.color,
    this.textAlign,
  }) : super(key: key);
  final String text;
  final Color? color;
  final TextAlign? textAlign;

  @override
  Widget build(BuildContext context) {
    return Text(
      text,
      textAlign: textAlign,
      style: TextStyle(
        fontSize: 32,
        fontWeight: kFW600,
        color: color ?? AppColors.textBlackColor,
      ),
    );
  }
}

class TextPOP32W400 extends StatelessWidget {
  ///Poppins Size: 32, Weight: [kFW400], Default Color: AppColors.textBlackColor
  const TextPOP32W400(
    this.text, {
    Key? key,
    this.color,
    this.textAlign,
  }) : super(key: key);
  final String text;
  final Color? color;
  final TextAlign? textAlign;

  @override
  Widget build(BuildContext context) {
    return Text(
      text,
      textAlign: textAlign,
      style: TextStyle(
        fontSize: 32,
        fontWeight: kFW400,
        color: color ?? AppColors.textBlackColor,
      ),
    );
  }
}

class TextPOP28W800 extends StatelessWidget {
  ///Poppins Size: 28, Weight: [kFW800], Default Color: AppColors.textBlackColor
  const TextPOP28W800(
    this.text, {
    Key? key,
    this.color,
    this.textAlign,
  }) : super(key: key);
  final String text;
  final Color? color;
  final TextAlign? textAlign;

  @override
  Widget build(BuildContext context) {
    return Text(
      text,
      textAlign: textAlign,
      style: TextStyle(
        fontSize: 28,
        fontWeight: kFW800,
        color: color ?? AppColors.textBlackColor,
      ),
    );
  }
}

class TextPOP24W700 extends StatelessWidget {
  ///Poppins Size: 24, Weight: [kFW700], Default Color: AppColors.textBlackColor
  const TextPOP24W700(
    this.text, {
    Key? key,
    this.color,
    this.textAlign,
  }) : super(key: key);
  final String text;
  final Color? color;
  final TextAlign? textAlign;

  @override
  Widget build(BuildContext context) {
    return Text(
      text,
      textAlign: textAlign,
      style: TextStyle(
        fontSize: 24,
        fontWeight: kFW700,
        color: color ?? AppColors.textBlackColor,
      ),
    );
  }
}

class TextPOP22W600 extends StatelessWidget {
  ///Poppins Size: 22, Weight: [kFW600], Default Color: AppColors.textBlackColor
  const TextPOP22W600(
    this.text, {
    Key? key,
    this.color,
    this.textAlign,
  }) : super(key: key);
  final String text;
  final Color? color;
  final TextAlign? textAlign;

  @override
  Widget build(BuildContext context) {
    return Text(
      text,
      textAlign: textAlign,
      style: TextStyle(
        fontSize: 22,
        fontWeight: kFW600,
        color: color ?? AppColors.textBlackColor,
      ),
    );
  }
}

class TextPOP20W800 extends StatelessWidget {
  ///Poppins Size: 20, Weight: [kFW800], Default Color: AppColors.textBlackColor
  const TextPOP20W800(
    this.text, {
    Key? key,
    this.color,
    this.textAlign,
  }) : super(key: key);
  final String text;
  final Color? color;
  final TextAlign? textAlign;

  @override
  Widget build(BuildContext context) {
    return Text(
      text,
      textAlign: textAlign,
      style: TextStyle(
        fontSize: 20,
        fontWeight: kFW800,
        color: color ?? AppColors.textBlackColor,
      ),
    );
  }
}

class TextPOP20W700 extends StatelessWidget {
  ///Poppins Size: 20, Weight: [kFW700], Default Color: AppColors.textBlackColor
  const TextPOP20W700(
    this.text, {
    Key? key,
    this.color,
    this.textAlign,
  }) : super(key: key);
  final String text;
  final Color? color;
  final TextAlign? textAlign;

  @override
  Widget build(BuildContext context) {
    return Text(
      text,
      textAlign: textAlign,
      style: TextStyle(
        fontSize: 20,
        fontWeight: kFW700,
        color: color ?? AppColors.textBlackColor,
      ),
    );
  }
}

class TextPOP20W400 extends StatelessWidget {
  ///Poppins Size: 20, Weight: [kFW400], Default Color: AppColors.textBlackColor
  const TextPOP20W400(
    this.text, {
    Key? key,
    this.color,
    this.textAlign,
  }) : super(key: key);
  final String text;
  final Color? color;
  final TextAlign? textAlign;

  @override
  Widget build(BuildContext context) {
    return Text(
      text,
      textAlign: textAlign,
      style: TextStyle(
        fontSize: 20,
        fontWeight: kFW400,
        color: color ?? AppColors.textBlackColor,
      ),
    );
  }
}

class TextPOP20W500 extends StatelessWidget {
  ///Poppins Size: 20, Weight: [kFW400], Default Color: AppColors.textBlackColor
  const TextPOP20W500(
      this.text, {
        Key? key,
        this.color,
        this.textAlign,
      }) : super(key: key);
  final String text;
  final Color? color;
  final TextAlign? textAlign;

  @override
  Widget build(BuildContext context) {
    return Text(
      text,
      textAlign: textAlign,
      style: TextStyle(
        fontSize: 20,
        fontWeight: kFW500,
        color: color ?? AppColors.textBlackColor,
      ),
    );
  }
}

class TextPOP18W800 extends StatelessWidget {
  ///Poppins Size: 18, Weight: [kFW800], Default Color: AppColors.textBlackColor
  const TextPOP18W800(
    this.text, {
    Key? key,
    this.color,
    this.textAlign,
  }) : super(key: key);
  final String text;
  final Color? color;
  final TextAlign? textAlign;

  @override
  Widget build(BuildContext context) {
    return Text(
      text,
      textAlign: textAlign,
      style: TextStyle(
        fontSize: 18,
        fontWeight: kFW800,
        color: color ?? AppColors.textBlackColor,
      ),
    );
  }
}

class TextPOP18W700 extends StatelessWidget {
  ///Poppins Size: 18, Weight: [kFW700], Default Color: AppColors.textBlackColor
  const TextPOP18W700(
    this.text, {
    Key? key,
    this.color,
    this.textAlign,
  }) : super(key: key);
  final String text;
  final Color? color;
  final TextAlign? textAlign;

  @override
  Widget build(BuildContext context) {
    return Text(
      text,
      textAlign: textAlign,
      style: TextStyle(
        fontSize: 18,
        fontWeight: kFW700,
        color: color ?? AppColors.textBlackColor,
      ),
    );
  }
}
class TextPOP18W500 extends StatelessWidget {
  ///Poppins Size: 18, Weight: [kFW700], Default Color: AppColors.textBlackColor
  const TextPOP18W500(
    this.text, {
    Key? key,
    this.color,
    this.textAlign,
  }) : super(key: key);
  final String? text;
  final Color? color;
  final TextAlign? textAlign;

  @override
  Widget build(BuildContext context) {
    return Text(
      text ?? "",
      textAlign: textAlign,
      style: TextStyle(
        fontSize: 18,
        fontWeight: kFW500,
        color: color ?? AppColors.textBlackColor,
      ),
    );
  }
}

class TextPOP18W300 extends StatelessWidget {
  ///Poppins Size: 18, Weight: [kFW300], Default Color: AppColors.textBlackColor
  const TextPOP18W300(
    this.text, {
    Key? key,
    this.color,
    this.textAlign,
  }) : super(key: key);
  final String? text;
  final Color? color;
  final TextAlign? textAlign;

  @override
  Widget build(BuildContext context) {
    return Text(
      text ?? "",
      textAlign: textAlign,
      style: TextStyle(
        fontSize: 18,
        fontWeight: kFW300,
        color: color ?? AppColors.textBlackColor,
      ),
    );
  }
}

class TextPOP16W600 extends StatelessWidget {
  ///Poppins Size: 16, Weight: [kFW600], Default Color: AppColors.textBlackColor
  const TextPOP16W600(
    this.text, {
    Key? key,
    this.color,
    this.textAlign,
  }) : super(key: key);
  final String text;
  final Color? color;
  final TextAlign? textAlign;

  @override
  Widget build(BuildContext context) {
    return Text(
      text,
      textAlign: textAlign,
      style: TextStyle(
        fontSize: 16,
        fontWeight: kFW600,
        color: color ?? AppColors.textBlackColor,
      ),
    );
  }
}

class TextPOP16W400 extends StatelessWidget {
  ///Poppins Size: 16, Weight: [kFW400], Default Color: AppColors.textBlackColor
  const TextPOP16W400(this.text,
      {Key? key, this.color, this.textAlign, this.maxLine, this.style})
      : super(key: key);
  final String text;
  final int? maxLine;
  final Color? color;
  final TextAlign? textAlign;
  final TextStyle? style;

  @override
  Widget build(BuildContext context) {
    return Text(
      text,
      textAlign: textAlign,
      maxLines: maxLine,
      style: style ??
          TextStyle(
            fontSize: 16,
            fontWeight: kFW400,
            color: color ?? AppColors.textBlackColor,
          ),
    );
  }
}
class TextPOP16W500 extends StatelessWidget {
  ///Poppins Size: 16, Weight: [kFW500], Default Color: AppColors.textBlackColor
  const TextPOP16W500(this.text,
      {Key? key, this.color, this.textAlign, this.maxLine, this.style})
      : super(key: key);
  final String text;
  final int? maxLine;
  final Color? color;
  final TextAlign? textAlign;
  final TextStyle? style;

  @override
  Widget build(BuildContext context) {
    return Text(
      text,
      textAlign: textAlign,
      maxLines: maxLine,
      style: style ??
          TextStyle(
            fontSize: 16,
            fontWeight: kFW500,
            color: color ?? AppColors.textBlackColor,
          ),
    );
  }
}

class TextPOP17W500 extends StatelessWidget {
  ///Poppins Size: 16, Weight: [kFW400], Default Color: AppColors.textBlackColor
  const TextPOP17W500(this.text,
      {Key? key, this.color, this.textAlign, this.maxLine, this.style})
      : super(key: key);
  final String text;
  final int? maxLine;
  final Color? color;
  final TextAlign? textAlign;
  final TextStyle? style;

  @override
  Widget build(BuildContext context) {
    return Text(
      text,
      textAlign: textAlign,
      maxLines: maxLine,
      style: style ??
          TextStyle(
            fontSize: 17,
            fontWeight: kFW500,
            color: color ?? AppColors.textBlackColor,
          ),
    );
  }
}

class TextPOP14W600 extends StatelessWidget {
  ///Poppins Size: 14, Weight: [kFW600], Default Color: AppColors.textBlackColor
  const TextPOP14W600(
    this.text, {
    Key? key,
    this.color,
    this.textAlign = TextAlign.center,
  }) : super(key: key);
  final String text;
  final Color? color;
  final TextAlign textAlign;

  @override
  Widget build(BuildContext context) {
    return Text(
      text,
      textAlign: textAlign,
      style: TextStyle(
        fontSize: 14,
        fontWeight: kFW600,
        color: color ?? AppColors.textBlackColor,
      ),
    );
  }
}

class TextPOP14W400 extends StatelessWidget {
  ///Poppins Size: 14, Weight: [kFW400], Default Color: AppColors.textBlackColor
  const TextPOP14W400(
    this.text, {
    Key? key,
    this.color,
    this.textAlign = TextAlign.center,
  }) : super(key: key);
  final String text;
  final Color? color;
  final TextAlign textAlign;

  @override
  Widget build(BuildContext context) {
    return Text(
      text,
      textAlign: textAlign,
      style: TextStyle(
        fontSize: 14,
        fontWeight: kFW400,
        color: color ?? AppColors.textBlackColor,
      ),
    );
  }
}

class TextPOP14W300 extends StatelessWidget {
  ///Poppins Size: 20, Weight: [kFW300], Default Color: AppColors.textBlackColor
  const TextPOP14W300(
    this.text, {
    Key? key,
    this.color,
    this.textAlign,
  }) : super(key: key);
  final String text;
  final Color? color;
  final TextAlign? textAlign;

  @override
  Widget build(BuildContext context) {
    return Text(
      text,
      textAlign: textAlign,
      style: TextStyle(
        fontSize: 14,
        fontWeight: kFW300,
        color: color ?? AppColors.textBlackColor,
      ),
    );
  }
}

class TextPOP13W600 extends StatelessWidget {
  ///Poppins Size: 13, Weight: [kFW600], Default Color: AppColors.textBlackColor
  const TextPOP13W600(
    this.text, {
    Key? key,
    this.color,
    this.textAlign = TextAlign.center,
  }) : super(key: key);
  final String text;
  final Color? color;
  final TextAlign textAlign;

  @override
  Widget build(BuildContext context) {
    return Text(
      text,
      textAlign: textAlign,
      style: TextStyle(
        fontSize: 13,
        fontWeight: kFW600,
        color: color ?? AppColors.textBlackColor,
      ),
    );
  }
}

class TextPOP12W400 extends StatelessWidget {
  ///Poppins Size: 12, Weight: [kFW400], Default Color: AppColors.textBlackColor
  const TextPOP12W400(
    this.text, {
    Key? key,
    this.color,
    this.textAlign = TextAlign.center,
    this.maxLines = 1,
  }) : super(key: key);
  final String text;
  final Color? color;
  final int? maxLines;
  final TextAlign textAlign;

  @override
  Widget build(BuildContext context) {
    return Text(
      text,
      textAlign: textAlign,
      maxLines: maxLines,
      softWrap: true,
      overflow: TextOverflow.ellipsis,
      style: TextStyle(
        fontSize: 12,
        fontWeight: kFW400,
        color: color ?? AppColors.textBlackColor,
      ),
    );
  }
}
